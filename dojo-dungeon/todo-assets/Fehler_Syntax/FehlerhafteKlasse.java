/**
 * In dieser Klasse sollen unterschiedliche Methoden implementiert werden, die fehlerhaft sind. Das
 * Verhalten der Methoden wird dann überprüft werden.
 */
public class FehlerhafteKlasse {

  /**
   * Diese Methode soll 2 zu {@code in} addieren und das Ergebnis in einen String umwandeln und
   * zurückgeben.
   *
   * <p>Wenn {@code in} {@code null} oder keine Zahl ist, soll "NaN" zurückgeben werden.
   *
   * <p>Wenn das Ergebnis größer als {@link Integer#MAX_VALUE} ist, soll "Integer overflow"
   * zurückgeben werden.
   *
   * @param in Eingabe, die geparst werden soll
   * @return Die Summe der Addition als String
   * @see Integer
   */
  public static String incrementByTwo(String in) {
    return null;
  }

  /**
   * Diese Methode soll die Fibonacci-Zahl für {@code n} berechnen und zurückgeben.
   *
   * <p>Wenn {@code in} {@code null} oder keine Zahl ist, soll "NaN" zurückgeben werden.
   *
   * <p>Wenn {@code in} kleiner 0 oder größer als 10 ist, soll "Too large" zurückgeben werden.
   *
   * @param n Eingabe, die geparst werden soll
   * @return Die Fibonacci-Zahl als String
   */
  public static String calculateFibonacci(String n) {
    return null;
  }
}
