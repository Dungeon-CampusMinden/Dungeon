package components;

import core.Component;

/**
 * Marks an entity as blocking.
 *
 * <p>If an entity is blocking, the player cannot enter the tile where this entity is placed.
 */
public final class BlockComponent implements Component {}
