package contrib.utils.components.skill;

/**
 * Enumeration of different skill-slots.
 *
 * <p>
 *   Skill slots are designed to allow a skill to be assigned to each mouse button,
 *   which can then be switched between them as desired.
 * </p>
 */
public enum SkillSlot {
  FIRST_SLOT,
  SECOND_SLOT
}
