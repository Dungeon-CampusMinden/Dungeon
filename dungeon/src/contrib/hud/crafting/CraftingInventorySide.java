package contrib.hud.crafting;

/** Inventory side involved in a crafting dialog transfer. */
public enum CraftingInventorySide {
  /** Target inventory that receives crafted items and provides source items. */
  TARGET,
  /** Crafting inventory that holds the items used as crafting input. */
  CRAFTING
}
