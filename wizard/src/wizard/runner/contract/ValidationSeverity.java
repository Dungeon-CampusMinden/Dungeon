package wizard.runner.contract;

/** Ordered severities supported by the runner report contract. */
public enum ValidationSeverity {
  /** A blocking issue. */
  ERROR,
  /** A non-blocking issue. */
  WARNING
}
